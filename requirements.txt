# CS584 AI Capstone — Python dependencies
# Install: pip install -r requirements.txt
#
# Note: Ollama handles LLM inference (GPU) independently.
# sentence-transformers pulls torch automatically (CPU version is fine).

# --- LangChain ecosystem ---
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-experimental>=0.3.0
langchain-neo4j>=0.3.0
langchain-ollama>=0.2.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# --- ML / embeddings ---
sentence-transformers>=3.0.0
scikit-learn>=1.3.0
numpy>=1.24.0

# --- Utilities ---
python-dotenv>=1.0.0
pandas>=2.0.0
tiktoken>=0.5.0
requests>=2.31.0
pdfplumber>=0.10.0

# --- Zone 3: Leiden clustering (conditional import; Zone 3 falls back gracefully if missing) ---
python-igraph>=0.11.0
leidenalg>=0.10.0
